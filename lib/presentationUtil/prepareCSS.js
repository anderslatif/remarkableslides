

export default function lookupUniqueCSSStyles(fileName) {
    if (fileName === "Introduction.md") {
        // todo the idea is to allow for custom styling for individual slides
        // I can then put plain CSS in the string and it will be placed in the style tag
        return "";
    }
    return "";
}